{
  "name": "shiori-reader",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "samples": "python3 tools/make_test_books.py",
    "deploy": "sh tools/deploy.sh"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.127.0",
    "jszip": "^3.10.2"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
